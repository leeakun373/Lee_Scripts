-- @description RadialMenu Tool - 设置编辑器（兼容壳）
-- @about
--   为了降低维护成本，设置编辑器已迁移到 `src/settings/controller.lua`。
--   保留此文件仅用于兼容旧入口：`require('main_settings').show()`。

return require("settings.controller")
