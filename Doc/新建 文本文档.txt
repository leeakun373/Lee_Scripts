① 看当前状态
git status


👉 看你改了哪些文件

② 加入暂存区
git add .


（如果只想加某个文件）

git add src/main_runtime.lua

③ 提交（写一句说明）
git commit -m "一句话说明你改了什么"

④ 拉一下远端（防止被拒绝）
git pull --rebase

⑤ 推送到 GitHub
git push

🧠 推送失败时（别慌）
❌ 提示 rejected (fetch first)

👉 正常情况

照做：

git pull --rebase
git push

🧯 救命指令（很重要）
❗ 放弃所有本地修改（还没 commit）
git restore .

❗ 回到上一次提交（已经 commit 了）
git reset --hard HEAD

❗ 回滚到某个旧版本（高级但实用）
git log --oneline


找到那行：

f9140a5  Update markers tools and helpers


然后：

git reset --hard f9140a5